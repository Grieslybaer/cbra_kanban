# Add class name of the person (user) which the project belongs to (as string)
# (required) Uncomment the following and add the class name:
# KanbanBoard.user_class = "User"

# Add class name of the card (task) model for the project (as string)
# (required) Uncomment the following and add the class name:
# KanbanBoard.card_class = "Card"

# Add an array of the states for the project (including strings)
# (not required) Uncomment the following and add the array:
# KanbanBoard.states = ["backlog", "todo", "in progress", "code review", "done"]